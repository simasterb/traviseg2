version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: 'Block #1'
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - checkout
  - name: tests
    task:
      prologue:
        commands:
          - checkout
      jobs:
        - name: unit tests
          commands:
            - echo "running simasterb unit tests"
        - name: integration tests
          commands:
            - echo "running integration tests"
